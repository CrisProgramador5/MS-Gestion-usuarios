apiVersion: v1
kind: Service

metadata:
  name: gestionusuarios-service

spec:
  type: LoadBalancer

  selector:
    app: gestionusuarios

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080