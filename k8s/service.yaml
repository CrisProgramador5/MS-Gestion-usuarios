apiVersion: v1
kind: Service

metadata:
  name: gestionusuarios-service
  namespace: microservices-demo

spec:
  type: LoadBalancer

  selector:
    app: gestionusuarios

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080